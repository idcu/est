apiVersion: v1
kind: Service
metadata:
  name: est-web-app-service
  namespace: default
  labels:
    app: est-web-app
spec:
  type: ClusterIP
  selector:
    app: est-web-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    name: http
